name: TUF-on-CI signing event

permissions: {}

on:
  workflow_dispatch:
  push:
    branches: ['sign/**']
    paths: ['metadata/**', 'targets/**']

jobs:
  handle-signing-event:
    name: TUF-on-CI signing event
    runs-on: ubuntu-latest
    permissions:
      contents: write # for adding targets changes into the signing event branch
      issues: write
      actions: write # for dispatching another signing-event workflow
      pull-requests: write

    steps:
      - name: Signing event
        uses: theupdateframework/tuf-on-ci/actions/signing-event@d0c0bb3b957286948a10fd8bc21c49e3816a9241
        with:
          token: ${{ secrets.TUF_ON_CI_TOKEN || secrets.GITHUB_TOKEN }}
